---
version: "2"
plugins:
  bandit:
    enabled: true
  markdownlint:
    checks:
      MD002:
        enabled: false
    enabled: true
  pylint:
    enabled: true
    issue_override:
      severity: info
  pep8:
    enabled: true
    issue_override:
      severity: info
checks:
  method-lines:
    config:
      threshold: 100

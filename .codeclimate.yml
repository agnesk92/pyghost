---
version: "2"
plugins:
  bandit:
    enabled: true
  pylint:
    enabled: true
  markdownlint:
    enabled: true
    checks:
      MD002: # "First header should be a h1 header"
        enabled: false

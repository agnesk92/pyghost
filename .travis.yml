language: python

services:
  - docker

before-install:
- docker build . -t pyghost:v1.0
- docker run -ti pyghost:v1.0 "pytest"
#- docker run -ti pyghost:v1.0 "cd /app; pytest; flake8"

script:
  - pytest && flake*

notifications:
  email: agi.uqbar@gmail.com
